/** @type {import('tailwindcss').Config} */
export default {
  darkMode: 'class', // ← penting buat toggle dark mode via class
  content: ["./index.html", "./src/**/*.{js,jsx}"],
  theme: {
    extend: {},
  },
  plugins: [],
}
